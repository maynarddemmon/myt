# Normalize all text to LF in the repo and working tree.
* text=auto eol=lf

# Binary — never touch.
*.png binary
*.jpg binary
*.gif binary
*.ico binary

# Vendored / generated — leave byte-for-byte as delivered.
src/test/qunit/** -text
src/js/expression_parser/peggy-*.min.js -text
src/js/jsclass/core_ORIGINAL.js -text
